# Daily SEO Intelligence — 2026-08-19

Domain age: 59 days · Generated by `scripts/seo/analyze.mjs` · Read-only toward production.

> **Reporting windows do not align, by nature.** Search Console covers through
> **2026-08-17** (lag 2d); web analytics covers through
> **n/a**. Any comparison across the two is indicative only.

## Snapshot

| Source | Window | Key figures |
|---|---|---|
| Search Console | 2026-08-11 → 2026-08-17 | 518 impressions · 17 clicks · CTR 3.3% · pos 9.3 · 10 disclosed queries |
| Web analytics | n/a | _browser-gated; not collected_ |
| Internal links | 2026-08-19 | 28/28 crawled · 27 orphan(s) · depth 0 |
| PageSpeed / CrUX | n/a | _pending_ |
| Deployments | n/a | _not collected_ |
| SERP probe (benchmark, 48 queries) | 2026-08-19 | 6/48 top 20 · 6 top 10 · 4 top 3 |
| Discovery probe (evolving set) | 2026-08-19 | 0/6 top 20 · set v2 — **not comparable to the benchmark** |
| Technical | 2026-08-19 | 16/16 pages 200 · 65 issue(s) |

## Facts

- Search Console covers through **2026-08-17** (lag 2d). Latest reported day 2026-08-17: **84 impressions, 2 clicks**.
- Impressions/day so far: 100 → 155 → 106 → 76 → 101 → 55 → 51 → 45 → 84.
- 10 disclosed queries — 9 on-topic, 0 adjacent, 1 irrelevant (90% on-topic).
- 367 of 518 impressions come from queries Search Console will not name.
- Page exposure: `/blog/best-ai-tools-for-civil-engineers-in-2026` 179/5 · `/` 178/8 · `/blog/ai-reporting-software-civil-engineers` 61/1 · `/team` 49/1 · `/reportly` 35/0 · `/blog/civil-engineering-report-automation-vs-manual` 24/0 · `/services` 22/0 · `/blog` 17/0 · `/team?member=Zander Dent` 16/1 · `/contact?mode=email&contact=omair@vibeops.ca` 1/1 (impressions/clicks).
- Fixed SERP probe: 6/48 in top 20 · 6 top 10 · 4 top 3.
- Indexation: **36 URLs indexed**, 17 not indexed against 28 in the sitemap (Search Console, through 2026-08-17).
- **Discovery** probe (evolving set v2, NOT the benchmark): 0/6 in top 20 · 0 top 10. Per-cluster: blog 0/5 · sitewide 0/1.
- Internal link graph: 28/28 sitemap URLs crawled · 27 orphan(s) · max click depth 0.

## Interpretation

- CTR 3.3% at position 9.3 is within the normal band for that position — no snippet problem indicated. (Volume has cleared the 100-impression gate, so this reading is now meaningful.)
- The discovery probe sees us nowhere in the top 20 for any of its 6 queries, while Search Console reports impressions for several of them at positions 9–47. Both can be true: Search Console positions are averages across a window, the probe is one point-in-time read from one location, and most of those positions are beyond the top 20 the probe measures. **Search Console remains authoritative.**
- PageSpeed is unavailable today because the shared anonymous quota was exhausted. Set PAGESPEED_API_KEY in .env for a dedicated free quota. Nothing is wrong with the site.
- 64 further per-page findings (missing canonical, zero h1, identical title and description) are suppressed because they are all downstream of the un-prerendered shell above, not independent defects. They will resolve together when the shell does, and they are listed in full in `docs/seo/data/daily/2026-08-19.json` under technical.data.issues.

## New search behaviour

- **2 newly disclosed queries:** `ai software for civil engineering` (1, on_topic), `noboru yonemitsu` (4, irrelevant)

## Opportunities

| Score | Opportunity | Cluster | Impr 7d | Pos | Days | Confidence | Action | Status |
|---|---|---|---|---|---|---|---|---|
| 24.9 | `best ai for civil engineering` | sitewide | 20 | — | 2 | LOW | HOLD | WATCHING |
| 23.7 | `best ai tools for civil engineers` | — | 27 | — | 2 | LOW | HOLD | WATCHING |
| 22.8 | `vibe ops` | — | 19 | — | 2 | LOW | HOLD | WATCHING |
| 21.5 | `vipbops` | — | 11 | — | 2 | LOW | HOLD | WATCHING |
| 5.4 | `civil engineering ai tools` | sitewide | 6 | — | 2 | LOW | HOLD | WATCHING |
| 4.9 | `reportly` | — | 7 | — | 2 | LOW | HOLD | WATCHING |
| 4.6 | `ai tools for civil engineers` | — | 4 | — | 2 | LOW | HOLD | WATCHING |
| 4.4 | `ai tool for civil engineering` | sitewide | 1 | — | 2 | LOW | HOLD | WATCHING |

**2 READY for the weekly gate:**
- `16 URLs: 16 tracked URLs return BYTE-IDENTICAL html (md5 1d201f7d). The site is serving an un-prerendered SPA shell, so on a crawler's first pass every one of these URLs has the same title, the same content and no links. Google does render JS, so this is not fatal, but it delays indexing, spends crawl budget on duplicates, and makes per-page metadata invisible until render. Fix is prerendering or SSR, not a metadata change.` — Technical defect on 16 URLs: 16 tracked URLs return BYTE-IDENTICAL html (md5 1d201f7d). The site is serving an un-prerendered SPA shell, so on a crawler's first pass every one of these URLs has the same title, the same content and no links. Google does render JS, so this is not fatal, but it delays indexing, spends crawl budget on duplicates, and makes per-page metadata invisible until render. Fix is prerendering or SSR, not a metadata change.
- `16 URLs: 16 tracked URLs return BYTE-IDENTICAL html (md5 6a64cf3f). The site is serving an un-prerendered SPA shell, so on a crawler's first pass every one of these URLs has the same title, the same content and no links. Google does render JS, so this is not fatal, but it delays indexing, spends crawl budget on duplicates, and makes per-page metadata invisible until render. Fix is prerendering or SSR, not a metadata change.` — Technical defect on 16 URLs: 16 tracked URLs return BYTE-IDENTICAL html (md5 6a64cf3f). The site is serving an un-prerendered SPA shell, so on a crawler's first pass every one of these URLs has the same title, the same content and no links. Google does render JS, so this is not fatal, but it delays indexing, spends crawl budget on duplicates, and makes per-page metadata invisible until render. Fix is prerendering or SSR, not a metadata change.

## Risks and issues

- **Google has indexed 8 more URLs than the sitemap advertises** (36 vs 28). The sitemap is a suggestion, not a boundary. The excess is usually retired URLs still being served or parameterised variants of a real page. Identify them in the Page indexing report before assuming they are harmless: an indexed URL we no longer maintain can outrank the page meant to replace it.
- **5 page(s) excluded by a `noindex` tag.** That is our directive, not Google's decision. Confirm every one of them is deliberately excluded.
- **5 page(s) are "Duplicate without user-selected canonical".** On an un-prerendered SPA this is the expected downstream symptom: Google saw identical HTML at several URLs and picked one. It should resolve when the shell does.
- **1 page(s) reported as Soft 404** — a URL returning 200 with content Google reads as "not found". On a client-rendered SPA this is often a real route rendering an empty shell.
- **TECHNICAL** `16 URLs` — 16 tracked URLs return BYTE-IDENTICAL html (md5 6a64cf3f). The site is serving an un-prerendered SPA shell, so on a crawler's first pass every one of these URLs has the same title, the same content and no links. Google does render JS, so this is not fatal, but it delays indexing, spends crawl budget on duplicates, and makes per-page metadata invisible until render. Fix is prerendering or SSR, not a metadata change. (critical)

## Recommendation today

**TECHNICAL — investigate now** — 1 technical issue(s) at high/critical severity. This is the one category that may justify same-day remediation.

## Production action

**INVESTIGATE — see technical risks above.**

A verified technical defect is the only condition under which this system may touch production on a daily run. Confirm the defect manually before changing anything.
