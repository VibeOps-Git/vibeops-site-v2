# Lightweight requirements for Vercel deployment (under 250MB)
# This replaces heavy libraries with efficient alternatives

# Data processing (replaces pandas + numpy)
polars==0.20.3

# Plotting (replaces plotly)
matplotlib==3.8.2

# Database connection (keep if needed)
psycopg2-binary==2.9.9

# Web utilities
requests==2.31.0
python-dotenv==1.0.0

# If you need basic math operations without numpy
# Built-in Python math module is sufficient for most cases

Flask==3.0.2
supabase==1.2.0
flask-oauthlib
openai
authlib
Werkzeug==3.0.1
Jinja2==3.1.3
itsdangerous==2.1.2
click==8.1.7
python-dateutil==2.8.2
pytz==2024.1
six==1.16.0
tenacity==8.2.3
typing_extensions>=4.11,<5
urllib3==2.2.1
httpx==0.24.1
postgrest==0.10.8